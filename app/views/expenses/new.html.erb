<% content_for :title, "Add expense" %>

<%= native_form_tag %>
<%= native_navbar_tag("Add expense") do |navbar| %>
  <% navbar.submit_button title: "Add", click: "#expense-submit" %>
<% end %>

<header class="native:hidden mb-[18px] flex items-center gap-1.5">
  <%= link_to "‹ Cancel", root_path, class: "px-0.5 py-1 text-[15px] font-semibold text-muted" %>
  <h1 class="text-xl font-bold tracking-tight">Add expense</h1>
</header>

<div class="rounded-2xl border border-line bg-surface p-4 shadow-card">
  <%= render "form" %>
</div>
